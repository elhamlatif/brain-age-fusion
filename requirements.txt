Tested with Python 3.10 (should also work fine on 3.11)

numpy>=1.24
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3
nibabel>=5.1
mne>=1.6
torch>=2.1
matplotlib>=3.7
tqdm>=4.66

Only needed for download_data.py
No version pin — hasn't caused trouble so far
openneuro-py